/**
 * Iconițe SVG (Lucide) pentru cele 5 domenii CFD — folosite în header și în dropdown Asset.
 */
import { Landmark, Bitcoin, Gem, LineChart, Building2 } from 'lucide-react';

/** id din CFD_NAV_GROUPS → componentă icon */
export const CFD_DOMAIN_ICONS = {
  forex: Landmark,
  crypto: Bitcoin,
  commodities: Gem,
  indices: LineChart,
  equities: Building2,
};
