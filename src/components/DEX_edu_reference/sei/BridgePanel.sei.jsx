import React from "react";

/**
 * 🌉 BridgePanel.sei.jsx - SEI Network Bridge Panel
 * 
 * Component pentru bridging între SEI și alte blockchain-uri:
 * - EVM ↔ SEI bridging
 * - Solana ↔ SEI bridging
 * - Stacks ↔ SEI bridging
 * - Cross-chain transfers via IBC (Inter-Blockchain Communication)
 * - Bridge status tracking
 * 
 * @module BridgePanelSei
 */

export default function BridgePanelSei() {
  return (
    <div>
      <h2>Bridge Panel - SEI</h2>
      <p>This is a placeholder UI for the bridge component on SEI blockchain.</p>
      {/* TODO: Inject OTA zone + multi-chain support */}
    </div>
  );
}
